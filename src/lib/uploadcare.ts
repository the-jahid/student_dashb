import { UploadClient } from "@uploadcare/upload-client"

// Initialize Uploadcare client with the provided API key
export const uploadcareClient = new UploadClient({
  publicKey: "938dc22edc5227866907",
})
